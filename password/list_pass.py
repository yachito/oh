# Silahkan Jika Ingin Mengubah Listpass Nya
# Jika Ingin Mengubah pw_tambahan Harap Gunakan Pemisah Menggunakan |
pw_tambahan="""

bismillah|sayang|bangsat|anjing|kontol|freefire|MobileLegends||Gamers|Gaming|Alok123|Indonesia|Rahasia|Kentod|Sultan|Ganteng|Cantik

""".strip().split("|")
ngntd=((())>(()))+((())>(()))
def awok(ngentod):
	return [ngentod,ngentod+"123",ngentod+"12345"]
def pw_list(ngentot):
	if len(ngentot)==2:
		wans_gans=ngentot[1].split(" ")
		if len(wans_gans[ngntd])!=ngntd:
			asu=awok(wans_gans[ngntd])
			if len(wans_gans[ngntd])<=5:
				del asu[ngntd]
				for x in pw_tambahan: asu.append(x)
			else:
				for x in pw_tambahan: asu.append(x)
		else: asu=pw_tambahan
	else: asu=pw_tambahan
	return asu